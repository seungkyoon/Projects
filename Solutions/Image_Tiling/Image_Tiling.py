from PIL import Image, ImageDraw
import heapq

def average_color((x, y), (a, b), image):
	r, g, b = 0, 0, 0
	count = 0
	for s in range(x, x+a):

def MSE():
	pass

self.image = Image.open(path).convert('RGB')
